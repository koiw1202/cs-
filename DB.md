# cs-
cs지식

# DB

무결성 제약조건: 데이터의 안정성을 보장하기 위한 여러가지 조건
1. 개체 무결성: 기본키는 null 또는 중복 값으로 구성될 수 없다.
2. 참조 무결성: 외래키는 null 또는 참조되는 릴레이션의 기본키 값과 동일해야한다.
3. 도메인 무결성: 각 속성들의 값은 정의된 도메인에 속한 값이어야 합니다. (성별: 남,여 값만 올 수 있다.)

트랜잭션의 4가지 속성(ACID)
1. 원자성: All or Nothing. 트랜잭션 내 실행한 작업들은 모두 성공 또는 모두 실패
2. 일관성: 일관성있는 데이터베이스 상태를 유지해야한다. 트랜잭션은 무결성 제약조건을 충족해야한다.
3. 격리성: A라는 트랜잭션이 특정 레코드를 점유하고 있는데 동시에 다른 B라는 트랜잭션이 접근할 수 없다.
4. 지속성: 트랜잭션이 성공적으로 끝나면 그 내용은 저장되어야 한다.

격리 수준
Read Uncommitted
1. 커밋하지 않은 데이터를 읽을 수 있다.
2. 트랜잭션 1이 수정중이고 커밋하지 않아도 트랜잭션 2가 데이터를 조회할 수 있다.

Read Committed
1. 커밋한 데이터만 읽을 수 있다.
2. Dirty read가 발생하지 않음
3. NON - Repeatable Read가 발생 할 수도 있다.
4. 트랜잭션 1이 회원 A를 조회 중인데 트랜잭션 2가 회원 A를 수정하고 커밋하면 트랜잭션 1이 다시 회원 A를 조회했을 때 수정된 데이터가 조회된다.

Repeatable Read
1. 한 번 조회한 데이터를 반복해서 조회해도 같은 데이터가 반복해서 조회된다.
2. Phantom read가 발생
3. 반복 조회시 결과 집합이 달라지는 것을 PHANTOM READ라 한다.

Serializable
1. 가장 엄격한 트랜잭션 격리 수준
2. Phantom read가 발생하지 않음.
3. 신형 트랜잭션이 읽은 데이터를 후행 트랜잭션이 갱신하거나 삭제하지 못할 뿐만 아니라 중간에 새로운 레코드를 산입하는 것도 막아주는 격리수준. 가장 엄격한 격리 수준이다.

트랜잭션 격리에 따른 문제점 항목

Dirty Read
1. 아직 커밋되지 않은 수정 중인 데이터를 다른 트랜잭션에서 읽을 수 있도록 허용할 때 발생한다.
2. 

Non-Repeatable Read
1. 한 트랜잭션 내에서 같은 쿼리를 두번 수행할 때, 그 사이에 다른 트랜잭션이 값을 수정 또는 삭제함으로써 두 쿼리가 상이하게 나타나는 비 일관성이 발생하는 것을 말한다.

Phantom Read
1. 한 트랜잭션 안에서 일정범위의 레코드를 두번 이상 읽을 때, 첫 번재 쿼리에서 없던 유령 레코드가 두번째 쿼리에서 나타나는 현상을 말한다.



























